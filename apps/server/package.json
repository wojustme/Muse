{
  "name": "@muse/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:app": "pnpm run dev",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@muse/model-router": "workspace:*",
    "@muse/shared": "workspace:*",
    "ai": "^5.0.0",
    "dotenv": "^16.0.0",
    "drizzle-orm": "^0.36.0",
    "fastify": "^5.0.0",
    "mysql2": "^3.11.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
